services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    container_name: uptime-kuma
    restart: always
    networks:
      - npm_network
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./data:/app/data

networks:
  npm_network:
    external: true
